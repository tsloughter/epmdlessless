{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, {epmdlessless, "0.1.0"},
         [epmdlessless,
          sasl]},

        {mode, dev},

        {vm_args_src, "config/vm.args.src"}
]}.

{profiles, [{prod, [{relx, [{mode, prod}]}]}]}.
